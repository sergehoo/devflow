Bonjour {{ recipient.get_full_name|default:recipient.username }},

{% if assigned_by %}{{ assigned_by.get_full_name|default:assigned_by.username }} vous a confié{% else %}Une nouvelle tâche vous a été confiée{% endif %} sur Dev'Flow :

  « {{ task.title }} »
  Projet : {{ project.name }}
  {% if task.priority %}Priorité : {{ task.get_priority_display }}{% endif %}
  {% if task.due_date %}Échéance : {{ task.due_date|date:"d/m/Y" }}{% endif %}

Ouvrez-la pour découvrir le détail et commencer à avancer :

    /tasks/{{ task.pk }}/

Bonne mission,
L'équipe Dev'Flow
